# macOS / Xcode
.DS_Store
*.xcuserstate
xcuserdata/
DerivedData/
build/
*.ipa

# NOTE: NaviAI.xcodeproj and NaviAI/Info.plist are COMMITTED so CodeMagic's
# auto-detection recognizes this repository as an iOS application and can build
# it without any extra generation step. Do not gitignore them.
# project.yml is kept in the repo only as a documented XcodeGen spec for
# regenerating the .xcodeproj by hand later.

# CocoaPods / SPM
Pods/

# Ruby / CodeMagic local
vendor/

# Editor
.vscode/
.idea/

*.log
